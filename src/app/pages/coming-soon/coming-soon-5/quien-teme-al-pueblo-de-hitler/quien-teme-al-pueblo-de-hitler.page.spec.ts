import { ComponentFixture, TestBed } from '@angular/core/testing';
import { QuienTemeAlPuebloDeHitlerPage } from './quien-teme-al-pueblo-de-hitler.page';

describe('QuienTemeAlPuebloDeHitlerPage', () => {
  let component: QuienTemeAlPuebloDeHitlerPage;
  let fixture: ComponentFixture<QuienTemeAlPuebloDeHitlerPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(QuienTemeAlPuebloDeHitlerPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
